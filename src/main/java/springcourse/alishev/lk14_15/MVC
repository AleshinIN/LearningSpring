MVC - это паттерн проектирования, который состоит из Controller(DispatcherServlet выбирается контроллер и на него отправляется HTTP-запрос),
который взаимодействует с View(что-то типа веба) и Model(что-то типа бека). View и Model напрямую не взаимодействуют(если брать WEB MVC).
Model взаимодействует с базой данных